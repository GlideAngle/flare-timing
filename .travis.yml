# This is the simple Travis configuration, which is intended for use
# on applications which do not require cross-platform and
# multiple-GHC-version support. For more information and other
# options, see:
#
# https://docs.haskellstack.org/en/stable/travis_ci/
#
# Copy these contents into the root directory of your Github project in a file
# named .travis.yml

# Use new container infrastructure to enable caching
sudo: false

# Do not choose a language; we provide our own build tools.
language: generic

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.stack

# Ensure necessary system libraries are present
addons:
  apt:
    packages:
      - libgmp-dev

before_install:
# To avoid running out of memory, add a swap file.
# SEE: https://www.reddit.com/r/haskell/comments/5ov5k1/how_to_stack_build_specifically_lens_on_a_low/
- sudo mkdir -v /var/cache/swap
- cd /var/cache/swap
- sudo dd if=/dev/zero of=swapfile bs=1K count=4M
- sudo chmod 600 swapfile
- sudo mkswap swapfile
- sudo swapon swapfile
# Download and unpack the stack executable
- mkdir -p ~/.local/bin
- export PATH=$HOME/.local/bin:$PATH
- travis_retry curl -L https://get.haskellstack.org/stable/linux-x86_64.tar.gz | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'

install:
- stack --no-terminal --install-ghc build siggy-chardust detour-via-sci detour-via-uom flight-igc flight-kml --only-dependencies

script:
# Build the package, its tests, and its docs and run the tests
- stack --no-terminal build siggy-chardust detour-via-sci detour-via-uom flight-igc flight-kml
